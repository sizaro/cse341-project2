<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px;
            max-width: 90%;
            text-align: center;
        }

        h1 {
            color: #333;
        }

        form {
            margin-top: 20px;
        }

        form label {
            display: block;
            margin-bottom: 8px;
            color: #555;
        }

        form input[type="text"],
        form input[type="password"],
        button {
            width: 100%;
            padding: 10px;
            margin-top: 8px;
            margin-bottom: 16px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        hr {
            margin: 20px 0;
            border: none;
            border-top: 1px solid #eee;
        }

        .oauth-buttons {
            display: flex;
            justify-content: space-between;
        }

        .oauth-buttons button {
            width: calc(50% - 5px);
        }

        .oauth-buttons button:first-child {
            margin-right: 10px;
        }

        .oauth-buttons button:last-child {
            margin-left: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Sign In</h1>
        
        <!-- Sign in with username and password -->
        <form action="/signin" method="POST">
            <h2>Sign In with Username/Email and Password</h2>
            <label for="username">Username/Email:</label>
            <input type="text" id="username" name="username" required>
            <br>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required>
            <br>
            <button type="submit">Sign In</button>
        </form>

        <hr>

        <!-- OAuth Signing-in options -->
        <div class="oauth-buttons">
            <!-- Sign in with Google -->
            <a href="/auth/google" style="text-decoration: none;">
                <button style="background-color: #dd4b39;">Sign In with Google</button>
            </a>

            <!-- Signing in with GitHub -->
            <a href="/auth/github" style="text-decoration: none;">
                <button style="background-color: #333;">Sign In with GitHub</button>
            </a>
        </div>
    </div>
</body>
</html>
